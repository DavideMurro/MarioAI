import { TextToImageResponse } from './text-to-image-response.model';

describe('TextToImageResponse', () => {
  it('should create an instance', () => {
    expect(new TextToImageResponse()).toBeTruthy();
  });
});
